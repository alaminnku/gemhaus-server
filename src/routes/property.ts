import { Router } from 'express';

const router = Router();

// Create a property
router.post('/', async (req, res) => {});

// Get all properties
router.get('/', async (req, res) => {});

export default router;
